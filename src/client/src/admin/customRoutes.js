import React from 'react';
import { Route } from 'react-router-dom';
import { AdminList } from './resources/Admins';
import { LocalesList } from './resources/Locales';

export default [
/*     <Route exact path="/locales" component={(props) => <LocalesList {...props}/>} />,
 */];